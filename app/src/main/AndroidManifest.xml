<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.evenion.evenionapp">

    <application
        android:name=".StudentApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EvenionApp">
        <activity
            android:name=".views.activities.base.BaseActivity"
            android:label="@string/title_activity_base"
            android:theme="@style/Theme.EvenionApp.NoActionBar"></activity>
        <activity android:name=".views.activities.detail.DetailActivity" />
        <activity android:name=".views.activities.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>